import { ViewStyle } from "react-native";

const space = [0, 4, 8, 12, 20, 32, 64];

const container: ViewStyle = {
  flex: 1,
};

const fontSize = {
  small: 12,
  normal: 14,
  large: 16,
  title: 24,
};

const borderRadius = {
  small: 4,
  normal: 8,
  large: 12,
};

const gstyle = {
  space,
  container,
  fontSize,
  borderRadius,
};

export default gstyle;
